# Required for the RSVP form to submit responses
# Create a Google Apps Script web app that receives POST requests and saves to a Google Sheet
PUBLIC_RSVP_ENDPOINT="https://script.google.com/macros/s/YOUR_SCRIPT_ID_HERE/exec"

# Optional: Override site URL (default: https://sergioybarbara.com)
# SITE_URL="http://localhost:4321"

# Optional: Override base path (default: /)
# BASE_PATH="/"

# Image style mode: 'drawing' for artistic drawings, 'photo' for real photos
# Controls which version of location images are displayed (ceremony, reception)
PUBLIC_IMAGE_STYLE="drawing"
